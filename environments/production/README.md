# Production environment

Compose production-owned infrastructure here from reusable modules when the resource is not already owned by a provider-native Git integration root. Keep one lifecycle owner per resource graph.
